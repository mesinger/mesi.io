version: '3.7'

services:
  mesiclipboarddb:
    container_name: 'mesiclipboarddb'
    image: 'mysql:8.0.18'
    ports:
      - 3306:3306
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=mesiclipboarddb